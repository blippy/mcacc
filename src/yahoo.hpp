#pragma once
//#ifndef YAHOO_HPP
//#define YAHOO_HPP

#include <string>
#include <vector>


std::vector<std::string> fetch_tickers(const std::vector<std::string>& tickers, std::string& usd);

//#endif
